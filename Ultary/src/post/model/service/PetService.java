package post.model.service;

import static common.JDBCTemplate.*;

import java.sql.Connection;

import member.model.vo.Pet;
import post.model.dao.PostDAO;

public class PetService {

	/*
	 * public int insertPost(Pet p) { Connection conn = getConnection();
	 * 
	 * PostDAO dao = new PostDAO();
	 * 
	 * int result1 = dao.insertPost(conn, p);
	 * 
	 * if(result1 > 0 && result2 > 0) { commit(conn); } else { rollback(conn); }
	 * close(conn);
	 * 
	 * return result1; }
	 */
	
}
